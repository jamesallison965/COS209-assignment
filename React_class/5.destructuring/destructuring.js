// Destructuring
// Allows extracting values from arrays or objects easily.

const person = { fname: "Alice", age: 22 };

// Destructuring object
const { fname, age } = person;

console.log(fname); //will display Alice
console.log(age);  //will display 22

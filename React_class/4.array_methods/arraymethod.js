// Array Methods
// Built-in methods to work with arrays easily. eg map, filter, reduce, find, etc. 

// Example: map - transform each element
const numbers = [1, 2, 3, 4];
const doubled = numbers.map(num => num * 2);

console.log(doubled); //it will display [2, 4, 6, 8]

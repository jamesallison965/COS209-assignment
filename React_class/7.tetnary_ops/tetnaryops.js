// Ternary Operator
// Shorthand or fast way for if...else statements.

const age = 18;

// condition ? valueIfTrue : valueIfFalse
const canVote = age >= 18 ? "Yes, you can vote" : "No, too young";

console.log(canVote); // Yes, you can vote
